# 目付 Metsuke

Premiere Pro 向け UXP プラグイン。指定した監視フォルダ内のメディアファイルを、Premiere Pro のプロジェクトビンに自動で読み込む。
Boothで無料配布中（応援版300円を併売）。就職・転職活動のポートフォリオを兼ねる。

- 構成：`index.html`（HTML/CSS/JS を1ファイルに集約）、`manifest.json`、`package.json`
- 対象：Premiere Pro 2025（v25.6以降）/ 2026、Windows
- ブランド：kamneko／江戸役職シリーズ。漢字表記＋ローマ字を必ず併記
- プラグインID：`com.kamneko.metsuke`
- 現在のバージョン：v1.3.3
- GitHub：https://github.com/kamneko88/metsuke（**Public**、2026-07-07公開）

## ⚠️ このリポジトリはPublicです（最重要・必読）

蔵方・勘定吟味役・差配役など他のプロジェクトは現在Privateだが、Metsukeは既にGitHub上で一般公開済み。**コミットした内容は即座に誰でも閲覧可能になる。**

- 個人パス（`D:\Data\...`、`C:\Users\...`）、個人情報、実メールアドレスをコード・README・コミットメッセージのいずれにも書かない
- `git config user.email` はリポジトリ単位でGitHubのnoreplyアドレスに設定済み。**変更しない・上書きしない**
- 新規バイナリ（`.ccx`等）や大容量ファイルをコミットしない（`dist/`はgitignore対象）
- 迷ったら実装前にコミット内容の要約を提示し、確認を取ってからコミットすること

## 開発ルール

- **日本語で応答する**
- **実装前に必ず方針を提示し、「OK」等の明示的な承認を得てからコードを書く**
- コードの提示は不要。変更内容の概要説明のみでよい
- 不足情報は仮定せず、必ず確認してから進める
- 根本原因の分析を投機的修正より優先する。推測で修正を重ねない
- コミット前に必ず `git status` を確認し、意図しないファイルが含まれていないかチェックする
- コミットメッセージは日本語＋英語併記（例：`docs: update README / READMEを更新`）

## 引き継ぎ・ドキュメント

### ドキュメントの分担（UXPシリーズ共通・2026-08-12 再編）

| ファイル | 内容 |
|---|---|
| `../uxp_common_notes.md` | UXP開発の共通知見（落とし穴・座標系・API・UI設計・配布）。**着手前に読む** |
| `../adobe_uxp_projects.md` | シリーズの方針・構想・運用方針 |
| `_local/metsuke_dev_notes.md` | このプラグインの進捗・仕様・実測値・バックログ |
| `../../開発状況サマリー.md` | 全プロジェクトの現在地（状態・バージョン・次のアクション） |

**同じ情報を2箇所に書かない。** 状態・バージョンは開発状況サマリーのみに書く。
新しく判明したUXPの知見は `../uxp_common_notes.md` へ集約する。

### ドキュメント更新は Claude Code が行わない（2026-08-12 決定）

| 担当 | 作業 |
|---|---|
| **Claude Code** | 実装 → **Git のコミット・プッシュまで**。ここで完了報告して終わる |
| **claude.ai チャット側（監督役）** | コミット・プッシュの完了を確認したうえで、各種ドキュメントを更新 |

- **「今日はここまで」「作業終了」等と言われても、md ファイルの更新を始めないこと。**
  コミット・プッシュが済んでいるかを確認し、済んでいれば完了を報告して終わる
- 例外：`CLAUDE.md` など**リポジトリ内のファイルへの追記を明示的に依頼された場合のみ**
- 理由：チャット側は仕様決定の経緯・判断理由・実機確認の結果を保持しているため記録の質が高い。
  両方が書くと二重管理になり、内容が食い違う

### その他

- `_local/` は gitignore 済み
- ファイル名に日付を付けない。ファイル内に「最終更新日」を記載し、更新時は必ず日付も更新する

## UXP の落とし穴（Metsukeで確認済み）

- `<button>` タグは UXP の Spectrum ウィジェットにスタイルを上書きされる → `<div role="button">` で代替
- `border-radius` はボタン要素に対してUXP内部でオーバーライドされる
- `Notification` API は UXP 環境では非対応（`ReferenceError: Notification is not defined`）。デスクトップ通知機能はv1.3.2で削除済み、復活させない
- `manifestVersion` は `5` を使用。`7`にすると認識されない
- `manifest.json` のルートに `"main": "index.html"` を置く（`package.json`側の`main`は削除する）
- `FolderItem.cast()` は例外ではなく `null` を返す場合がある → 必ず null チェック
- シーケンスのトラック取得（`getVideoTrackCount()` `getVideoTrack()` 等）は全て `await` が必要
- `getTrackItems()` は同期メソッド（await不要）
- 削除処理は1アイテムずつ個別transactionが安全（まとめると途中エラーで残りがスキップされる）
- フォルダのD&D登録はUXPの制限により不可。クリックダイアログで代替
- 監視フォルダ内のファイル削除・移動はビンに反映されない（UXP API制約、既知の制限として明記済み）

## 動作確認について

Premiere Pro での実際のフォルダ監視・自動読み込み動作の確認はユーザーしかできない。
実機確認が必要な項目は明示して依頼すること。
